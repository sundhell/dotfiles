- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.alacritty.yml:
    ~/.ctags:
    ~/.gemrc:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.mailcap:
    ~/.msmtprc:
    ~/.notmuch-config:
    ~/.offlineimaprc:
    ~/.pentadactylrc:
    ~/.rspec:
    ~/.signature:
    ~/.slate:
    ~/.tmux.conf:
    ~/.tridactylrc:
    ~/.urlview:
    ~/.vimperatorrc:
